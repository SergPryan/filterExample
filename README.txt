1. Create a mysql database named demo. Database connection settings in the src / main / resources / database.properties file
2. To create tables and fill tables, the file src / main / resources / create.sql
3. To build the project you need a maven. In the folder execute the command mvn package. The war folder appears in the target folder.
4. War file must be installed in servlet container tomcat version not less 8